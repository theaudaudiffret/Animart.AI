# Frontend (Vite) — variables publiques embarquées au build.
# La clé anon est publique par design (protégée par la RLS Postgres).
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
